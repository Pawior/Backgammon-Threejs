import Models from "./js/Models.js";
import Game from "./js/Game.js";

let models;
let game;

window.addEventListener("load", function () {
  // models = new Models();
  game = new Game();
  // models.initalizeScene();
  // models.handleWindowResize();
  // models.addBoard();
  // models.render();
  // models.getCamera().position.y = 10;
  // game.addClickListener(models.getScene(), models.getCamera());
  // let checkers = game.getCheckers();
  // models.addCheckers(checkers);
});
